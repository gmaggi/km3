You have two options to visualize the Sphinx-Web of this Online Reconstruction System documentation

1.- (Desired)

Go to the webside: https://common.pages.km3net.de/onlineSoftware/

2.- If you want to compile it in you local machine, you need:

In the confi.py file edit:
plantuml = 'java -jar /PATH_TO/plantuml.jar'

and set:

export GRAPHVIZ_DOT=/PATH_TO/dot <- binary file 

make html

Then grab the output /PATH_TO/index.html file and put it in your browser